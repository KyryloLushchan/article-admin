// Скопируй этот файл в config.js и подставь свои значения.
// config.js в .gitignore и НЕ коммитится.
//
// Используем ТОЛЬКО anon / publishable ключ — он публичный, его можно светить.
// service_role НЕ использовать никогда.

const SUPABASE_URL = "https://YOUR-PROJECT-ref.supabase.co";
const SUPABASE_ANON_KEY = "YOUR-ANON-OR-PUBLISHABLE-KEY";
